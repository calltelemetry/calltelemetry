# NATS Server Configuration
listen: 0.0.0.0:4222

# HTTP monitoring — enables /healthz endpoint for Docker healthcheck
http_port: 8222

jetstream {
  store_dir: /jetstream/store
  max_mem: 256MB
  max_file: 1GB
}
