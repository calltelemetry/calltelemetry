# Maximum payload
max_payload: 8MB

# HTTP monitoring port — required for Docker healthcheck (/healthz)
http_port: 8222

jetstream {
  store_dir: "/jetstream/store"
  max_memory_store: 1GB
  max_file_store: 10GB
}
