global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: prometheus
    metrics_path: /prometheus/metrics
    static_configs:
      - targets: ['localhost:9090']

  # Uncomment these jobs when the web service exposes a /metrics endpoint:
  # - job_name: cdrcisco-web
  #   metrics_path: /metrics
  #   static_configs:
  #     - targets: ['web:4080']
  #   metric_relabel_configs:
  #     - source_labels: [__name__]
  #       regex: 'curri_.*'
  #       action: keep

  # - job_name: cdrcisco-api
  #   metrics_path: /metrics
  #   static_configs:
  #     - targets: ['web:4000']
